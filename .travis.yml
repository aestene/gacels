language: python
python:
  - "3.6"      # current default Python on Travis CI

# command to install dependencies
install:
  - pip install -r requirements.txt

script:
  - pylint src
  - pylint tests
  - python -m pytest -rA
  - pytest --cov=src/AesLib tests/